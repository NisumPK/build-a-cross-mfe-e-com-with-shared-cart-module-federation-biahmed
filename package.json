{
  "name": "mfe-ecommerce-shared-cart",
  "private": true,
  "version": "0.0.0",
  "description": "Dev convenience launcher only — each app (host/catalog-mfe/cart-mfe/wishlist-mfe/auth-mfe) remains independent with its own package.json and can be run/deployed on its own.",
  "scripts": {
    "dev": "concurrently -k -n catalog,cart,wishlist,auth,host -c blue,magenta,cyan,green,yellow \"npm --prefix catalog-mfe run dev\" \"npm --prefix cart-mfe run dev\" \"npm --prefix wishlist-mfe run dev\" \"npm --prefix auth-mfe run dev\" \"npm --prefix host run dev\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "dependencies": {
    "@reduxjs/toolkit": "^2.2.7"
  }
}
