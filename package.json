{
  "name": "mfe-ecommerce-shared-cart",
  "private": true,
  "version": "0.0.0",
  "description": "Dev convenience launcher only — each app (host/catalog-mfe/cart-mfe) remains independent with its own package.json and can be run/deployed on its own.",
  "scripts": {
    "dev": "concurrently -k -n catalog,cart,host -c blue,magenta,yellow \"npm --prefix catalog-mfe run dev\" \"npm --prefix cart-mfe run dev\" \"npm --prefix host run dev\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "dependencies": {
    "@reduxjs/toolkit": "^2.2.7"
  }
}
